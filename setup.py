# This file is intentionally minimal.
# All build configuration is in pyproject.toml (PEP 517/518).
# Kept only for compatibility with very old pip versions (<21.3) that
# do not support pyproject.toml-based builds.
#
# DO NOT add configuration here — use pyproject.toml instead.
from setuptools import setup

setup()
