"""\nVerbose Mode & Educational Features\n====================================\n\nLearn about distributions while fitting! Verbose mode provides\nplain-language explanations of every step.\n\nUse Case:\n---------\nYou're learning statistics, teaching, or want to understand\nwhat's happening under the hood.\n\nAuthor: Ali Sadeghi Aghili\n\"\"\"\n\nimport numpy as np\nfrom distfit_pro import get_distribution, DistFitter\nfrom distfit_pro.core.config import config\n\nprint(\"═" * 70)\nprint(\"📚 VERBOSE MODE & EDUCATIONAL FEATURES\")\nprint(\"═" * 70)\n\n\n# ============================================================================\n# EXAMPLE 1: Basic Verbose Fitting\n# ============================================================================\n\nprint(\"\\n" + \"─" * 70)\nprint(\"Example 1: Fitting with Explanations\\n\")\n\n# Enable verbose mode\nconfig.set_verbosity('verbose')\n\nprint(\"🔊 VERBOSE MODE ENABLED\")\nprint(\"Watch as distfit-pro explains each step...\\n\")\nprint(\"═" * 70)\nprint()\n\n# Generate data\ndata = np.random.normal(100, 15, 500)\n\n# Fit with verbose mode\ndist = get_distribution('normal')\ndist.fit(data)  # Will show detailed explanations\n\nprint()\nprint(\"═" * 70)\nprint(\"✓ Fitting complete!\\n\")\n\n# Turn off verbose for cleaner output in next examples\nconfig.set_verbosity('normal')\n\n\n# ============================================================================\n# EXAMPLE 2: Learning About Distributions\n# ============================================================================\n\nprint(\"\\n" + \"═" * 70)\nprint(\"Example 2: Distribution Explanations\\n\")\n\n# Get educational info about distributions\ndists = ['normal', 'exponential', 'gamma', 'weibull', 'lognormal']\n\nfor name in dists:\n    dist = get_distribution(name)\n    print(f\"\\n📖 {dist.info.display_name}:\")\n    print(f\"   {dist.explain()}\")\n    print(f\"   Parameters: {', '.join(dist.info.parameters)}\")\n    print(f\"   Support: {dist.info.support}\")\n\n\n# ============================================================================\n# EXAMPLE 3: Verbose Comparison\n# ============================================================================\n\nprint(\"\\n" + \"═" * 70)\nprint(\"Example 3: Comparing Distributions (Verbose)\\n\")\n\nconfig.set_verbosity('verbose')\n\nprint(\"Generating right-skewed data (like income)...\\n\")\nincome_data = np.random.lognormal(10, 1, 300)\n\nprint(\"\\nTrying multiple distributions:\\n\")\nprint(\"═" * 70)\nprint()\n\nfitter = DistFitter(distributions=['normal', 'lognormal', 'gamma'])\nfitter.fit(income_data)  # Explains comparison process\n\nprint()\nprint(\"═" * 70)\nprint(\"\\nComparison complete!\\n\")\n\nconfig.set_verbosity('normal')\n\n\n# ============================================================================\n# EXAMPLE 4: Understanding Parameters\n# ============================================================================\n\nprint(\"\\n" + \"═" * 70)\nprint(\"Example 4: Parameter Interpretations\\n\")\n\ndef explain_parameters(dist):\n    \"\"\"Print user-friendly parameter explanations\"\"\"\n    print(f\"\\n{dist.info.display_name} Distribution:\\n\")\n    \n    if dist.info.name == 'normal':\n        print(f\"  μ (mu/loc):    {dist.params['loc']:.3f}\")\n        print(f\"                 → Center of the bell curve\")\n        print(f\"                 → Average value\")\n        print(f\"\\n  σ (sigma/scale): {dist.params['scale']:.3f}\")\n        print(f\"                 → Width of the bell curve\")\n        print(f\"                 → Typical deviation from mean\")\n        print(f\"                 → 68% of data within μ±σ\")\n        \n    elif dist.info.name == 'exponential':\n        rate = 1 / dist.params['scale']\n        print(f\"  λ (lambda/rate): {rate:.3f}\")\n        print(f\"                   → Event rate per unit time\")\n        print(f\"  Mean wait time:  {dist.params['scale']:.3f} units\")\n        print(f\"                   → Average time between events\")\n        \n    elif dist.info.name == 'gamma':\n        print(f\"  α (alpha/shape): {dist.params['alpha']:.3f}\")\n        print(f\"                   → Number of exponential stages\")\n        print(f\"  β (beta/scale):  {dist.params['beta']:.3f}\")\n        print(f\"                   → Scale of each stage\")\n        print(f\"  Mean:            {dist.mean():.3f}\")\n        print(f\"                   → α × β\")\n        \n    elif dist.info.name == 'weibull':\n        print(f\"  c (shape):       {dist.params['c']:.3f}\")\n        if dist.params['c'] < 1:\n            print(f\"                   → Decreasing failure rate\")\n        elif dist.params['c'] > 1:\n            print(f\"                   → Increasing failure rate (wear-out)\")\n        else:\n            print(f\"                   → Constant failure rate (exponential)\")\n        print(f\"  Scale:           {dist.params['scale']:.3f}\")\n        print(f\"                   → Characteristic lifetime\")\n\n\n# Examples\nprint(\"\\n1. Normal Distribution (symmetric):\")\ndata1 = np.random.normal(50, 10, 300)\ndist1 = get_distribution('normal')\ndist1.fit(data1)\nexplain_parameters(dist1)\n\nprint(\"\\n2. Exponential Distribution (memoryless):\")\ndata2 = np.random.exponential(5, 300)\ndist2 = get_distribution('exponential')\ndist2.fit(data2)\nexplain_parameters(dist2)\n\nprint(\"\\n3. Gamma Distribution (task durations):\")\ndata3 = np.random.gamma(3, 2, 300)\ndist3 = get_distribution('gamma')\ndist3.fit(data3)\nexplain_parameters(dist3)\n\n\n# ============================================================================\n# EXAMPLE 5: Interactive Learning\n# ============================================================================\n\nprint(\"\\n" + \"═" * 70)\nprint(\"Example 5: Learning Through Examples\\n\")\n\ndef demonstrate_distribution(name, data_generator, description):\n    \"\"\"Show distribution with context\"\"\"\n    print(f\"\\n{'='*70}\")\n    print(f\"📘 {name.upper()}\")\n    print(f\"{'='*70}\")\n    print(f\"\\n{description}\\n\")\n    \n    # Generate and fit\n    data = data_generator()\n    dist = get_distribution(name)\n    dist.fit(data)\n    \n    print(\"Fitted Parameters:\")\n    for param, value in dist.params.items():\n        print(f\"  {param}: {value:.3f}\")\n    \n    print(f\"\\nKey Statistics:\")\n    print(f\"  Mean:     {dist.mean():.3f}\")\n    print(f\"  Median:   {dist.median():.3f}\")\n    print(f\"  Std Dev:  {dist.std():.3f}\")\n    \n    try:\n        print(f\"  Skewness: {dist.skewness():.3f}\")\n    except:\n        pass\n    \n    print(f\"\\nTypical Questions:\")\n    \n    if name == 'normal':\n        p90 = dist.ppf(0.90)\n        print(f\"  • Top 10% threshold: {p90:.2f}\")\n        print(f\"  • P(X > {dist.mean():.1f}): 50% (symmetric!)\")\n        \n    elif name == 'exponential':\n        p_half = dist.cdf(dist.mean())\n        print(f\"  • P(wait < mean): {p_half*100:.1f}% (always 63.2%!)\")\n        print(f\"  • Median wait: {dist.median():.2f} (< mean due to skew)\")\n        \n    elif name == 'weibull':\n        print(f\"  • Median lifetime: {dist.median():.2f}\")\n        print(f\"  • 10% failure by: {dist.ppf(0.10):.2f}\")\n\n\n# Demonstrations\ndemonstrate_distribution(\n    'normal',\n    lambda: np.random.normal(70, 10, 500),\n    \"\"\"Use Case: Test scores, measurement errors, biological traits\nShape: Symmetric bell curve\nKey Property: Mean = Median = Mode\"\"\"\n)\n\ndemonstrate_distribution(\n    'exponential',\n    lambda: np.random.exponential(10, 500),\n    \"\"\"Use Case: Time between events (calls, failures, arrivals)\nShape: Decreasing exponential\nKey Property: Memoryless - past doesn't affect future\"\"\"\n)\n\ndemonstrate_distribution(\n    'weibull',\n    lambda: np.random.weibull(2.5, 500) * 1000,\n    \"\"\"Use Case: Product lifetimes, reliability engineering\nShape: Can model infant mortality, random failure, or wear-out\nKey Property: Shape parameter determines failure pattern\"\"\"\n)\n\n\n# ============================================================================\n# EXAMPLE 6: Verbosity Levels\n# ============================================================================\n\nprint(\"\\n" + \"═" * 70)\nprint(\"Example 6: Different Verbosity Levels\\n\")\n\ndata = np.random.gamma(2, 3, 300)\ndist = get_distribution('gamma')\n\nprint(\"Testing different verbosity levels:\\n\")\n\nlevels = ['silent', 'normal', 'verbose']\n\nfor level in levels:\n    print(f\"\\n{'─'*70}\")\n    print(f\"Level: {level.upper()}\\n\")\n    \n    config.set_verbosity(level)\n    \n    if level == 'silent':\n        print(\"(No output except errors)\")\n    \n    dist_test = get_distribution('gamma')\n    dist_test.fit(data)\n    \n    if level != 'verbose':\n        print(f\"✓ Fitting completed ({level} mode)\")\n\nconfig.set_verbosity('normal')\n\n\n# ============================================================================\n# KEY TAKEAWAYS\n# ============================================================================\n\nprint(\"\\n" + \"═" * 70)\nprint(\"🎓 KEY TAKEAWAYS\")\nprint(\"═" * 70)\nprint(\"\"\"\n1. VERBOSITY LEVELS:\n   - 'silent':  No output (only errors)\n   - 'normal':  Standard output (default)\n   - 'verbose': Detailed explanations of each step\n\n2. ENABLE VERBOSE MODE:\n   from distfit_pro.core.config import config\n   config.set_verbosity('verbose')\n\n3. LEARNING FEATURES:\n   - dist.explain(): What the distribution represents\n   - dist.info: Metadata (parameters, support, etc.)\n   - Verbose mode explains:\n     * Data characteristics\n     * Why certain methods chosen\n     * Parameter meanings\n     * Statistical interpretations\n\n4. BEST FOR:\n   - Learning statistics\n   - Teaching / tutoring\n   - Debugging fit issues\n   - Understanding model selection\n   - Building intuition\n\n5. MULTILINGUAL SUPPORT:\n   - Set language: config.set_language('fa') for Persian\n   - Supports: en, fa, de\n   - See examples/04_multilingual/ for details\n\n6. NEXT STEPS:\n   - Practice with real datasets\n   - Try examples/02_advanced/ for complex scenarios\n   - See documentation for full API reference\n   - Contribute translations or examples!\n\"\"\")

print(\"\\n" + \"═" * 70)\nprint(\"✅ BASIC EXAMPLES COMPLETE!\n\")\nprint(\"Next: Explore examples/02_advanced/ for:")
print(\"  • Weighted fitting (survey data)\")\nprint(\"  • Censored data (reliability)\")\nprint(\"  • Bootstrap confidence intervals\")\nprint(\"  • Mixture models\")\nprint(\"  • Custom optimization\")\nprint(\"═" * 70)\n\")\