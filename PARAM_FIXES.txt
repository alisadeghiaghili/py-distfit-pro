CRITICAL PARAM FIXES NEEDED:

1. WeibullDistribution (line ~254):
   - Change: self._params = {'alpha': shape, 'a': shape, 'scale': scale}
   - To: self._params = {'c': shape, 'scale': scale}
   - Also fix mode() and _get_scipy_params() to use 'c'

2. LognormalDistribution (line ~285): 
   - Change: self._params = {'alpha': shape, 'a': shape, 'scale': scale}
   - To: self._params = {'s': shape, 'scale': scale}
   - Also fix mode() and _get_scipy_params() to use 's'
   - Same for _fit_mom()

3. HypergeometricDistribution (line ~1000):
   - User passes N (population), n (draws)
   - But internally scipy needs M, n, N where:
     * M = population size
     * n = sample size 
     * N = number of success states in population
   - Current code: self._params = {'M': N, 'n': n, 'N': M}  <- WRONG ORDER!
   - Should calculate proper M value based on mean

Applying these fixes now...
